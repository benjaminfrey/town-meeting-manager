# Session 13.01 — Progressive Web App Setup + Web Push Notifications

**Phase:** 1 — MVP | **Block:** 13 — PWA & Push Notifications
**Dependencies:** 02.02
**Estimated tasks:** 17

---

## Description

Configure the Town Meeting Manager web application as a Progressive Web App (PWA) with a web app manifest, Service Worker for offline caching, app install prompt, and proper icons. Then extend the service worker to handle Web Push Notifications, including the push event listener, the push subscription API endpoints, the `usePushNotifications` hook, a notification permission prompt component, notification preferences UI, and the server-side push dispatch service.

## Tasks

1. Create web app manifest (manifest.json): name, short_name, description, icons, theme color, display: standalone, start_url
2. Create Service Worker using vite-plugin-pwa (injectManifest mode)
3. Configure cache strategies: CacheFirst for static assets, NetworkFirst for HTML and API
4. Implement offline fallback page: displayed when navigating to uncached routes while offline
5. Create app install prompt: detect beforeinstallprompt event, show custom install banner
6. Generate PWA icons in required sizes (192x192, 512x512, maskable)
7. Configure Vite build for PWA output (service worker registration, manifest injection)
8. Test PWA installability: Chrome DevTools → Application → Manifest, Lighthouse PWA audit
9. Verify: app installable on mobile and desktop, offline caching works, service worker registered, Lighthouse PWA score passes
10. Add push event handler to service worker (show notification, handle click)
11. Add push subscription API endpoints to packages/api
12. Create usePushNotifications hook
13. Create NotificationPermissionPrompt component
14. Create notification preference center additions
15. Create push dispatch service in API
16. Wire push notifications to existing notification events
17. Verify end-to-end push flow

## Prompt

```
You are configuring the Town Meeting Manager web application as a Progressive Web App (PWA) with Web Push Notifications. The app was set up in session 02.01 with React 19, React Router v7, Vite, Tailwind CSS v4, and shadcn/ui. React Query data layer was integrated in session 02.02 — server state is managed by TanStack Query with Supabase as the backend.

PWA ARCHITECTURE:
- vite-plugin-pwa provides the manifest and service worker infrastructure
- The service worker caches the application shell (HTML, JS, CSS, images)
- React Query provides stale-while-revalidate data caching in memory
- Together they give fast loads and reasonable offline resilience
- Full offline data access is NOT a goal — graceful degradation is sufficient
- Web Push Notifications are a Phase 1 feature (not "later") — push is part of the MVP

VAPID keys were generated in session 01.04 and stored in docker/.env.
The push_subscriptions table was created in session 01.04 migration.
The web-push package was installed in packages/api in session 01.04.

PROJECT CONTEXT:
- Monorepo: packages/web (React 19 + React Router v7 Framework Mode, Vite bundler), packages/api
- Target devices: laptops and tablets used by town clerks, iPads for meeting operations, phones for board members
- The app must be installable as a standalone app (display: standalone) on all platforms
- No React Native or native app — the PWA IS the mobile experience for Phase 1

---

TASK 1: Create web app manifest
File: packages/web/public/manifest.json

  {
    "name": "Town Meeting Manager",
    "short_name": "TownMeet",
    "description": "Municipal meeting management for Maine towns — agendas, minutes, and board operations",
    "start_url": "/dashboard",
    "display": "standalone",
    "background_color": "#ffffff",
    "theme_color": "#1e3a5f",
    "orientation": "any",
    "categories": ["government", "productivity"],
    "icons": [
      { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any" },
      { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any" },
      { "src": "/icons/icon-maskable-192.png", "sizes": "192x192", "type": "image/png", "purpose": "maskable" },
      { "src": "/icons/icon-maskable-512.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" }
    ],
    "screenshots": [],
    "scope": "/",
    "lang": "en-US",
    "dir": "ltr"
  }

The theme_color (#1e3a5f) is a professional dark blue appropriate for government software.
Link the manifest in the root HTML template: <link rel="manifest" href="/manifest.json">
Add Apple meta tags in the HTML head:
  <meta name="theme-color" content="#1e3a5f">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="default">
  <meta name="apple-mobile-web-app-title" content="TownMeet">
  <link rel="apple-touch-icon" href="/icons/icon-192.png">

---

TASK 2: Create Service Worker with vite-plugin-pwa (injectManifest mode)
Use injectManifest mode (not generateSW) because we need a custom service worker for push events.

Install:
  pnpm --filter @town-meeting/web add -D vite-plugin-pwa

Create the custom service worker source: packages/web/src/sw.ts
This file is compiled by vite-plugin-pwa and becomes the actual service worker.

  /// <reference lib="webworker" />
  import { cleanupOutdatedCaches, precacheAndRoute } from 'workbox-precaching';

  declare const self: ServiceWorkerGlobalScope;

  // Precache all assets injected by vite-plugin-pwa
  precacheAndRoute(self.__WB_MANIFEST);
  cleanupOutdatedCaches();

  // Skip waiting so new SW activates immediately after installation
  self.addEventListener('install', () => self.skipWaiting());
  self.addEventListener('activate', (event) => {
    event.waitUntil(self.clients.claim());
  });

  // Push and notificationclick handlers are added in Task 10

Configure in packages/web/vite.config.ts:
  import { VitePWA } from 'vite-plugin-pwa';

  VitePWA({
    registerType: 'autoUpdate',
    strategies: 'injectManifest',  // use our custom sw.ts
    srcDir: 'src',
    filename: 'sw.ts',
    manifest: false, // we manage manifest.json manually
    injectManifest: {
      globPatterns: ['**/*.{js,css,html,ico,png,svg,woff2}'],
    },
    devOptions: {
      enabled: true,  // enable SW in development for testing
      type: 'module',
    },
  })

---

TASK 3: Configure cache strategies
In packages/web/src/sw.ts, add Workbox runtime caching after the precache setup:

  import { registerRoute } from 'workbox-routing';
  import { CacheFirst, NetworkFirst, StaleWhileRevalidate } from 'workbox-strategies';
  import { ExpirationPlugin } from 'workbox-expiration';

  // Static assets (content-hashed filenames) — CacheFirst
  registerRoute(
    ({ request }) => ['style', 'script', 'worker'].includes(request.destination),
    new CacheFirst({
      cacheName: 'static-assets',
      plugins: [new ExpirationPlugin({ maxEntries: 100, maxAgeSeconds: 365 * 24 * 60 * 60 })],
    })
  );

  // HTML pages — NetworkFirst (get latest on online, fallback to cache offline)
  registerRoute(
    ({ request }) => request.destination === 'document',
    new NetworkFirst({
      cacheName: 'html-pages',
      plugins: [new ExpirationPlugin({ maxEntries: 20, maxAgeSeconds: 24 * 60 * 60 })],
    })
  );

  // Fastify API calls — NetworkFirst with timeout
  registerRoute(
    ({ url }) => url.pathname.startsWith('/api/'),
    new NetworkFirst({
      cacheName: 'api-responses',
      networkTimeoutSeconds: 3,
      plugins: [new ExpirationPlugin({ maxEntries: 50, maxAgeSeconds: 24 * 60 * 60 })],
    })
  );

  // Supabase REST and Auth APIs — NetworkOnly (do NOT cache auth/data calls)
  // React Query handles its own in-memory caching for Supabase data
  registerRoute(
    ({ url }) => url.pathname.startsWith('/rest/v1/') || url.pathname.startsWith('/auth/v1/'),
    new NetworkFirst({ networkTimeoutSeconds: 5 })
  );

  // Images (town seals, exhibits) — StaleWhileRevalidate
  registerRoute(
    ({ request }) => request.destination === 'image',
    new StaleWhileRevalidate({
      cacheName: 'images',
      plugins: [new ExpirationPlugin({ maxEntries: 50, maxAgeSeconds: 30 * 24 * 60 * 60 })],
    })
  );

Install workbox packages:
  pnpm --filter @town-meeting/web add -D workbox-precaching workbox-routing workbox-strategies workbox-expiration

---

TASK 4: Implement offline fallback page
Create: packages/web/public/offline.html

Content:
- Town Meeting Manager branding
- Message: "You're currently offline"
- Explanation: "This page isn't available offline. Your meeting data was last loaded from the server — it may be available if you navigate to the dashboard."
- "Go to Dashboard" button
- Inline CSS only (since external stylesheets may not be cached for this page)

Add to workbox config in sw.ts:
  import { NavigationRoute, registerRoute } from 'workbox-routing';

  // Serve offline.html for navigation requests when offline
  registerRoute(
    new NavigationRoute(
      new NetworkFirst({ cacheName: 'html-pages' }),
      { denylist: [/^\/api\//] }
    )
  );

---

TASK 5: Create app install prompt
File: packages/web/src/components/pwa/InstallBanner.tsx

- Listen for the beforeinstallprompt event (store the event reference)
- Show a custom install banner when the event fires
- Banner design: fixed at bottom of screen, non-blocking
  - Message: "Install Town Meeting Manager for quick access"
  - "Install" button — triggers deferredPrompt.prompt()
  - "Not now" dismiss button — hides for session
  - "Don't show again" — sets localStorage flag
- Show the banner only:
  - When not already in standalone mode: !window.matchMedia('(display-mode: standalone)').matches
  - After 30 seconds on the app
  - If not previously dismissed permanently
- For iOS (Safari doesn't support beforeinstallprompt):
  - Detect iOS: /iPhone|iPad|iPod/.test(navigator.userAgent)
  - Show instructions: "To install, tap the Share button and then 'Add to Home Screen'"
  - Include a visual guide (share icon illustration or text description)
- After successful installation: hide banner, show welcome toast

---

TASK 6: Generate PWA icons
Create packages/web/public/icons/ directory with:
  - icon-192.png: 192x192 pixels
  - icon-512.png: 512x512 pixels
  - icon-maskable-192.png: 192x192 with maskable safe zone
  - icon-maskable-512.png: 512x512 with maskable safe zone
  - favicon.ico: 32x32 / 16x16 multi-resolution
  - apple-touch-icon.png: 180x180

For MVP: create placeholder icons (simple colored square with "TM" text, or gavel icon).
Use the app theme color (#1e3a5f dark blue).
Maskable versions: icon centered in the safe zone (80% center), theme color background filling full canvas.

Add to HTML head:
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  <link rel="icon" type="image/png" sizes="32x32" href="/icons/icon-32.png">

---

TASK 7: Configure Vite build for PWA output
Ensure the vite-plugin-pwa configuration from Task 2 is complete.
Verify the build output includes:
  - sw.js (compiled from sw.ts) in the dist/ directory
  - manifest.json copied to dist/
  - All icons in dist/icons/

Create packages/web/src/components/pwa/UpdateNotification.tsx:
  - When vite-plugin-pwa detects a waiting service worker, show a toast:
    "A new version of Town Meeting Manager is available."
  - "Update now" button — calls skipWaiting and reloads
  - "Later" button — dismisses (update applies on next full page load)
  - Use the virtual:pwa-register or useRegisterSW hook from vite-plugin-pwa

---

TASK 8: Test PWA installability
Build and preview:
  pnpm --filter @town-meeting/web build && pnpm --filter @town-meeting/web preview

Chrome DevTools → Application tab:
  - Manifest: verify correct name, icons, theme color
  - Service Workers: verify registered and active
  - Cache Storage: verify precached assets present

Run Lighthouse PWA audit:
  - Target: all PWA criteria passing
  - Note: during development on localhost, HTTPS criteria may not pass — expected

Test offline:
  - Set "Offline" in DevTools Network tab
  - Navigate to /dashboard — app loads from precache
  - Navigate to an uncached route — offline.html displays

---

TASK 9: Verify PWA installation
Build and serve the production build.
- Verify manifest: Chrome DevTools → Application → Manifest
- Verify service worker: registered, active, precached assets in Cache Storage
- Verify installability: install prompt appears, app opens in standalone mode
- Verify offline caching: pages cached, React Query cache keeps data readable briefly
- Verify offline fallback: navigate to uncached route while offline → offline.html
- Verify install banner: appears after 30 seconds, dismisses correctly
- Verify update notification: deploy new version, update toast appears
- Run Lighthouse PWA audit — all criteria pass

VERIFICATION CHECKLIST (Tasks 1-9):
1. Manifest loads with name, icons, theme color, display: standalone
2. Service worker uses injectManifest mode with custom sw.ts
3. CacheFirst for static assets, NetworkFirst for HTML and API, NetworkOnly for Supabase APIs
4. Offline fallback page displays for uncached routes when offline
5. Install banner appears on mobile/desktop with timing and dismiss behavior
6. Icons render correctly at all sizes including maskable variants
7. Vite build produces sw.js with precached assets
8. Lighthouse PWA audit passes all criteria
9. App is installable and runs in standalone mode

---

TASK 10: Add push event handler to service worker
Add to packages/web/src/sw.ts:

  // Push notification handler
  self.addEventListener('push', (event: PushEvent) => {
    if (!event.data) return;

    const data = event.data.json() as {
      title: string;
      body: string;
      icon?: string;
      badge?: string;
      tag?: string;
      url?: string;
    };

    const notificationPromise = self.registration.showNotification(data.title, {
      body: data.body,
      icon: data.icon ?? '/icons/icon-192.png',
      badge: data.badge ?? '/icons/icon-maskable-192.png',
      tag: data.tag,
      data: { url: data.url ?? '/dashboard' },
    });

    event.waitUntil(notificationPromise);
  });

  // Notification click handler — open/focus the app at the notification's URL
  self.addEventListener('notificationclick', (event: NotificationClickEvent) => {
    event.notification.close();
    const url = event.notification.data?.url ?? '/dashboard';

    event.waitUntil(
      self.clients.matchAll({ type: 'window', includeUncontrolled: true }).then((clients) => {
        // If the app is already open, focus it and navigate
        for (const client of clients) {
          if ('focus' in client && 'navigate' in client) {
            return (client as WindowClient).focus().then((c) => c.navigate(url));
          }
        }
        // Otherwise, open a new window
        return self.clients.openWindow(url);
      })
    );
  });

The push payload JSON schema:
  {
    title: string,      // notification title
    body: string,       // notification body text
    icon?: string,      // icon URL (defaults to app icon)
    badge?: string,     // small badge icon URL
    tag?: string,       // used to replace previous notifications of the same type
    url?: string        // URL to open when notification is clicked
  }

---

TASK 11: Add push subscription API endpoints
Create packages/api/src/routes/notifications.ts (or add to existing notifications route):

  POST /api/notifications/push/subscribe
  - Requires auth middleware (Bearer JWT)
  - Body: { endpoint: string, keys: { p256dh: string, auth: string }, userAgent?: string }
  - Validate with Zod schema
  - Insert or update row in push_subscriptions table (upsert on user_account_id + endpoint)
  - Return 200 on success

  DELETE /api/notifications/push/unsubscribe
  - Requires auth middleware (Bearer JWT)
  - Body: { endpoint: string }
  - Delete row from push_subscriptions where user_account_id matches and endpoint matches
  - Return 204 on success

Zod validation schema:
  const pushSubscriptionSchema = z.object({
    endpoint: z.string().url(),
    keys: z.object({
      p256dh: z.string().min(1),
      auth: z.string().min(1),
    }),
    userAgent: z.string().optional(),
  });

The auth middleware extracts the user_account_id from the JWT.
Use the Supabase admin client (service role key) for the DB write to bypass RLS.

---

TASK 12: Create usePushNotifications hook
Create packages/web/src/hooks/usePushNotifications.ts:

  import { useState, useEffect } from 'react';
  import { useCurrentUser } from './useCurrentUser';

  export type NotificationPermission = 'default' | 'granted' | 'denied';

  export interface UsePushNotificationsReturn {
    isSupported: boolean;
    permission: NotificationPermission;
    isSubscribed: boolean;
    subscribe: () => Promise<void>;
    unsubscribe: () => Promise<void>;
    isLoading: boolean;
  }

  export function usePushNotifications(): UsePushNotificationsReturn {
    const isSupported = 'PushManager' in window && 'serviceWorker' in navigator;
    const [permission, setPermission] = useState<NotificationPermission>(
      isSupported ? Notification.permission : 'denied'
    );
    const [isSubscribed, setIsSubscribed] = useState(false);
    const [isLoading, setIsLoading] = useState(false);

    const vapidPublicKey = import.meta.env.VITE_VAPID_PUBLIC_KEY;

    // Check existing subscription on mount
    useEffect(() => {
      if (!isSupported) return;
      navigator.serviceWorker.ready.then((registration) => {
        registration.pushManager.getSubscription().then((sub) => {
          setIsSubscribed(!!sub);
        });
      });
    }, [isSupported]);

    const subscribe = async () => {
      if (!isSupported) return;
      setIsLoading(true);
      try {
        // Request permission if not granted
        if (Notification.permission !== 'granted') {
          const result = await Notification.requestPermission();
          setPermission(result);
          if (result !== 'granted') return;
        }

        // Get service worker registration and subscribe to push
        const registration = await navigator.serviceWorker.ready;
        const subscription = await registration.pushManager.subscribe({
          userVisibleOnly: true,
          applicationServerKey: urlBase64ToUint8Array(vapidPublicKey),
        });

        // Send subscription to API
        const keys = subscription.toJSON().keys!;
        await fetch('/api/notifications/push/subscribe', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${await getToken()}` },
          body: JSON.stringify({
            endpoint: subscription.endpoint,
            keys: { p256dh: keys.p256dh, auth: keys.auth },
            userAgent: navigator.userAgent,
          }),
        });

        setIsSubscribed(true);
      } finally {
        setIsLoading(false);
      }
    };

    const unsubscribe = async () => {
      if (!isSupported) return;
      setIsLoading(true);
      try {
        const registration = await navigator.serviceWorker.ready;
        const subscription = await registration.pushManager.getSubscription();
        if (subscription) {
          await subscription.unsubscribe();
          await fetch('/api/notifications/push/unsubscribe', {
            method: 'DELETE',
            headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${await getToken()}` },
            body: JSON.stringify({ endpoint: subscription.endpoint }),
          });
          setIsSubscribed(false);
        }
      } finally {
        setIsLoading(false);
      }
    };

    return { isSupported, permission, isSubscribed, subscribe, unsubscribe, isLoading };
  }

  // Helper: convert VAPID public key from base64url to Uint8Array
  function urlBase64ToUint8Array(base64String: string): Uint8Array {
    const padding = '='.repeat((4 - (base64String.length % 4)) % 4);
    const base64 = (base64String + padding).replace(/-/g, '+').replace(/_/g, '/');
    const rawData = window.atob(base64);
    return Uint8Array.from([...rawData].map((char) => char.charCodeAt(0)));
  }

  // Helper: get current auth token from Supabase session
  async function getToken(): Promise<string> {
    const { data: { session } } = await supabase.auth.getSession();
    return session?.access_token ?? '';
  }

---

TASK 13: Create NotificationPermissionPrompt component
Create packages/web/src/components/pwa/NotificationPermissionPrompt.tsx:

- Show after first login on a new device
  - Check localStorage: 'tmm-notification-prompt-dismissed'
  - Don't show if already subscribed (isSubscribed from usePushNotifications)
  - Don't show if permission is 'denied'
- Displayed as a non-blocking banner (bottom of screen, above footer)
- Banner content:
  - Icon: bell icon (shadcn/ui or Lucide)
  - Message: "Get notified about upcoming meetings and published minutes"
  - "Enable notifications" button → calls subscribe(), shows loading state
  - "Not now" button → dismisses for the session (sessionStorage)
  - "Don't ask again" → sets localStorage flag, never shows again
- On iOS (PWA must be installed first for push on iOS 16.4+):
  - Detect iOS and standalone mode: /iPhone|iPad|iPod/.test(navigator.userAgent)
  - If not in standalone mode: show message "Install the app first to enable notifications"
  - If in standalone mode (installed): show normal subscribe flow
- After successful subscribe: hide banner, show toast "Notifications enabled"
- Wrap in ErrorBoundary — push permission errors must not crash the app

Usage: Add to the root layout so it appears on all authenticated pages.

---

TASK 14: Create notification preference center
Add to the existing user settings page, or create packages/web/src/routes/settings.notifications.tsx:

Section: "Push Notifications"
- Show current subscription status (Enabled / Not enabled)
- Toggle to enable/disable push notifications (calls subscribe() or unsubscribe())
- Show current device/browser if subscribed (from userAgent stored in push_subscriptions)

Notification type toggles (stored in user_account.notification_preferences JSONB):
  - Meeting reminders (24h before meetings I'm involved with) — default: true
  - Agenda published — default: true
  - Minutes approved — default: true
  - Last-minute meeting changes — default: true
  - Straw poll invitations — default: false

Each toggle:
  - Uses a shadcn/ui Switch component
  - On change: useMutation to update user_account.notification_preferences
  - Disabled if push is not subscribed (grey out with tooltip: "Enable notifications first")

Notification preferences JSONB schema:
  {
    push_enabled: boolean,
    meeting_reminders: boolean,
    agenda_published: boolean,
    minutes_approved: boolean,
    meeting_changes: boolean,
    straw_poll_invitations: boolean
  }

---

TASK 15: Create push dispatch service in API
Create packages/api/src/lib/push.ts:

  import webpush from 'web-push';

  webpush.setVapidDetails(
    `mailto:${process.env.VAPID_CONTACT_EMAIL}`,
    process.env.VAPID_PUBLIC_KEY!,
    process.env.VAPID_PRIVATE_KEY!
  );

  export interface PushPayload {
    title: string;
    body: string;
    icon?: string;
    badge?: string;
    tag?: string;
    url?: string;
  }

  // Send a push notification to a single subscription
  export async function sendPushNotification(
    subscription: { endpoint: string; p256dh: string; auth: string },
    payload: PushPayload
  ): Promise<'sent' | 'expired'> {
    try {
      await webpush.sendNotification(
        {
          endpoint: subscription.endpoint,
          keys: { p256dh: subscription.p256dh, auth: subscription.auth },
        },
        JSON.stringify(payload)
      );
      return 'sent';
    } catch (err: any) {
      // 410 Gone = subscription expired/unsubscribed — clean it up
      if (err.statusCode === 410) return 'expired';
      throw err;
    }
  }

  // Dispatch push to all subscriptions for a user, removing expired ones
  export async function dispatchPushToUser(
    supabase: SupabaseClient,
    userId: string,
    payload: PushPayload
  ): Promise<void> {
    const { data: subscriptions } = await supabase
      .from('push_subscriptions')
      .select('endpoint, p256dh, auth')
      .eq('user_account_id', userId);

    if (!subscriptions?.length) return;

    const results = await Promise.allSettled(
      subscriptions.map((sub) => sendPushNotification(sub, payload))
    );

    // Clean up expired subscriptions
    const expiredEndpoints = subscriptions
      .filter((_, i) => {
        const result = results[i];
        return result.status === 'fulfilled' && result.value === 'expired';
      })
      .map((sub) => sub.endpoint);

    if (expiredEndpoints.length > 0) {
      await supabase
        .from('push_subscriptions')
        .delete()
        .in('endpoint', expiredEndpoints);
    }
  }

  // Dispatch push to all subscribed users in a town for a specific event type
  export async function dispatchPushToTown(
    supabase: SupabaseClient,
    townId: string,
    eventType: keyof typeof EVENT_TO_PREFERENCE,
    payload: PushPayload
  ): Promise<void> {
    const preferenceKey = EVENT_TO_PREFERENCE[eventType];

    // Find all user_accounts in this town with the preference enabled
    const { data: userAccounts } = await supabase
      .from('user_account')
      .select('id, notification_preferences')
      .eq('town_id', townId)
      .is('archived_at', null);

    if (!userAccounts?.length) return;

    // Filter by preference setting
    const eligibleUserIds = userAccounts
      .filter((ua) => {
        const prefs = ua.notification_preferences;
        return prefs?.push_enabled !== false && prefs?.[preferenceKey] !== false;
      })
      .map((ua) => ua.id);

    await Promise.allSettled(
      eligibleUserIds.map((userId) => dispatchPushToUser(supabase, userId, payload))
    );
  }

  const EVENT_TO_PREFERENCE = {
    meeting_reminder: 'meeting_reminders',
    agenda_published: 'agenda_published',
    minutes_approved: 'minutes_approved',
    meeting_cancelled: 'meeting_changes',
    straw_poll: 'straw_poll_invitations',
  } as const;

---

TASK 16: Wire push notifications to notification events
In packages/api/src/routes/notifications.ts (or wherever email notifications are dispatched):

After sending each Postmark email notification, also dispatch a push notification to users
with active push subscriptions:

  // Example: after sending "agenda published" email
  await sendAgendaPublishedEmail(recipients, meeting);
  // Also send push notification
  await dispatchPushToTown(supabase, townId, 'agenda_published', {
    title: 'Agenda Published',
    body: `The agenda for ${meeting.title} has been published.`,
    tag: `agenda-${meeting.id}`,
    url: `/meetings/${meeting.id}`,
  });

Wire up these notification events:
- agenda_published: when agenda status changes to 'published'
- minutes_approved: when minutes status changes to 'approved'
- meeting_cancelled: when meeting status changes to 'cancelled'
- meeting_reminder: scheduled job 24 hours before meeting start (add to the existing scheduler)

For meeting_reminder, add to the API's scheduled job system:
  // Run daily at 8am, dispatch reminders for meetings starting in 22-26 hours
  // This catches meetings across time zones within the 24-hour window
  const upcomingMeetings = await getUpcomingMeetings({ hoursAhead: 24 });
  for (const meeting of upcomingMeetings) {
    await dispatchPushToTown(supabase, meeting.town_id, 'meeting_reminder', {
      title: 'Meeting Tomorrow',
      body: `${meeting.title} is scheduled for ${formatMeetingTime(meeting)}`,
      tag: `reminder-${meeting.id}`,
      url: `/meetings/${meeting.id}`,
    });
  }

---

TASK 17: Verify end-to-end push flow
Start the app and API in dev mode (using the production build for the service worker):
  pnpm --filter @town-meeting/web build
  pnpm --filter @town-meeting/web preview &
  pnpm --filter @town-meeting/api dev

Test the complete push flow:

1. Open the app in Chrome and log in
2. The NotificationPermissionPrompt banner appears (or navigate to settings)
3. Click "Enable notifications" — browser permission dialog appears
4. Grant permission — browser subscribes to push
5. Subscription is POSTed to /api/notifications/push/subscribe
6. Verify subscription saved: check push_subscriptions table in Supabase
7. Trigger a push notification via a test endpoint:
   curl -X POST http://localhost:3001/api/test/push \
     -H "Authorization: Bearer <token>" \
     -d '{"title": "Test", "body": "Test notification"}'
8. Notification appears on the device
9. Click the notification — app opens to the correct URL
10. Navigate to settings — notification type toggles work
11. Click "Disable notifications" — unsubscribe called, subscription removed from DB
12. Verify unsubscribe removes the subscription from push_subscriptions

iOS testing (requires installed PWA on iOS 16.4+):
- Install the app to home screen
- Log in and follow the push subscribe flow
- Verify iOS push works (different push service endpoint than Chrome)

VERIFICATION CHECKLIST (Tasks 10-17):
10. push event listener in sw.ts shows notifications with correct title, body, icon
11. notificationclick handler opens app at notification's url
12. POST /api/notifications/push/subscribe saves to push_subscriptions
13. DELETE /api/notifications/push/unsubscribe removes subscription
14. usePushNotifications: isSupported, permission, subscribe/unsubscribe work
15. NotificationPermissionPrompt appears after login, dismisses correctly
16. iOS shows install-first message if not in standalone mode
17. Notification preferences page has toggles for all 5 notification types
18. dispatchPushToUser sends to all subscriptions, cleans up expired ones
19. Notification events (agenda_published, minutes_approved, etc.) trigger push
20. End-to-end: subscribe → trigger event → notification appears → click → correct page

FULL VERIFICATION CHECKLIST:
1. Manifest loads with name, icons, theme color, display: standalone
2. Service worker (injectManifest mode) registers and precaches the application shell
3. CacheFirst for static assets, NetworkFirst for HTML and API
4. Offline fallback page displays for uncached routes when offline
5. Install banner appears on mobile/desktop with proper timing and dismiss behavior
6. Icons render correctly at all sizes including maskable variants
7. Vite build produces sw.js with precached assets
8. Lighthouse PWA audit passes all criteria
9. App is installable and runs in standalone mode
10. Push event handler shows notifications correctly in sw.ts
11. notificationclick opens app at correct URL
12. Push subscription API endpoints save/delete from push_subscriptions table
13. usePushNotifications hook works end-to-end
14. NotificationPermissionPrompt component renders and dismisses correctly
15. Notification preferences UI persists settings to user_account
16. Push dispatch service sends to subscriptions, cleans up expired
17. Push notifications fire for agenda_published, minutes_approved, meeting_cancelled, meeting_reminder
```

## Commit Message

```
Configure PWA with manifest, service worker, offline caching, install prompt, icons, and Web Push notifications
```
