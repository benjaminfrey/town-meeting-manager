/**
 * PowerSync schema re-export from the shared package.
 *
 * The shared package defines the canonical PowerSync schema with all 17 tables.
 * This module re-exports it for use in the web app's PowerSync initialization.
 */

export { AppSchema } from "@town-meeting/shared/powersync";
